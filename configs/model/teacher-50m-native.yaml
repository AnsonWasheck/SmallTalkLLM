# Training infrastructure only — never a deployable SmallTalk model.
# Uses the immutable 4096-token student tokenizer for token-level KD.
name: teacher-50m-native
vocab_size: 4096
hidden_size: 512
num_attention_heads: 8
num_key_value_heads: 2
head_dim: 64
intermediate_size: 1408
num_layers: 17
max_position_embeddings: 1024
rms_norm_eps: 1.0e-5
rope_theta: 10000.0
tie_word_embeddings: true
expected_params: 50021888
param_tolerance: 0.0
