# core (required)
torch>=2.1
tokenizers>=0.15
safetensors>=0.4
pyyaml>=6.0

# optional
# datasets>=2.14      # only needed to pull DailyDialog / EmpatheticDialogues from the Hub
# wandb>=0.16         # optional logging; local JSON/CSV logging is always on
# pytest>=7           # tests
